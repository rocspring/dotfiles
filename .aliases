###############
# ls alias
###############
alias ll='ls -l'

alias la='ls -la'

###############
# cd alias
###############
alias ..='cd ../'
alias ....='cd ../../'

alias cws='cd ~/workspace'
alias cms='cd ~/myspace'
alias cls='cd ~/learnspace'

###############
# git alias
###############
alias gad='git status | git add . '

alias gb='git branch '

alias gcb='git checkout -b'

alias gci='git commit -a -n -m '

alias gco='git checkout '

alias gcl='git clone '

alias gsh='git show '

alias grep='grep --color'

alias gst='git status'

alias gph='git push origin'

alias gpl='git pull'

alias gdf='git diff'

alias gmg='git merge'

alias glg="git log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"

